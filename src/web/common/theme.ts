import { themeColors as colors } from './colors'
import * as breakpoints from './mediaBreakpoints'
import { ThemeInterface } from './styled-components'

const theme: ThemeInterface = {
  breakpoints,
  colors
}

export default theme
