import * as React from 'react'
import Form from './form'

const NewPopupAd: React.FunctionComponent = () => (
  // <CreatePopupAd.Component>
  //   {(createPopupAd, { data: createPopupAdData }) => (
  //     <>
  //       {createPopupAdData && `Created popupAd ${createPopupAdData.createPopupAd.id}`}
  //       {!createPopupAdData &&
            <Form
              // onSubmit={popupAd => {
              //   createPopupAd({ variables: popupAd }).catch() // TODO error handling
              // }}
            />
  //       }
  //     </>
  //   )}
  // </CreatePopupAd.Component>
)

export default NewPopupAd
